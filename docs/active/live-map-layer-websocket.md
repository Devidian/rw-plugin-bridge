# Live map layer support

The cross-repository objective, contract, migration, rollback, risks, and
validation checklist are maintained in `../../docs/active/live-player-position-sync.md`.

This repository owns the read-only overlay of short-lived Admin Utils runtime
player samples and remains the compatible REST source for map-relevant plugin
persistence.
